# Architecture

MathBinder uses a WordPress core plugin plus independently versioned curriculum content packs.

The original My Math Binder PDF is the curriculum blueprint. Existing content is preserved; missing content is developed only when needed.
